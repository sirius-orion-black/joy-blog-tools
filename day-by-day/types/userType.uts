/** 登录 / 绑定返回的用户数据 */
export type LoginResult = {
	avatar: string
	email: string
	id: number
	needBind: boolean
	nickname: string
	phone: string
	sex: number
	state: number
	token: string
	
}

/** 邮箱验证码返回 */
export type VerifyCodeData = {
	validTime : number;
};